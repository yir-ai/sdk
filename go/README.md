# Yir Go SDK

Go 1.25+；本地候选模块 github.com/sungerine/yir-sdk/go。远端尚未创建，此路径暂不能下载。

在本目录运行 go test -p 2 ./...。公开后调用方使用 go/vX.Y.Z 对应模块版本，不需要 Yir 私库凭据。

客户端支持报价、持久化授权后的幂等提交、任务查询/等待/取消、文件和 Webhook。报价成功不代表已经生成；静态合同不保证实时供给或最终计费。
